package com.example.gesturepulse.ui.theme

import androidx.compose.ui.graphics.Color

// --- NEON PALETTE ---

// Główny akcent (Neon Orange)
val NeonOrange = Color(0xFFFF5500)
val NeonOrangeDim = Color(0xFFCC4400) // Ciemniejsza wersja do stanów disabled/tła

// Kolory pomocnicze (Neon Blue & Green)
val NeonBlue = Color(0xFF00E5FF)
val NeonGreen = Color(0xFF76FF03)
val NeonRed = Color(0xFFFF1744)

// Tła (Głęboka czerń i ciemne szarości)
val BlackBackground = Color(0xFF050505) // Prawie idealna czerń
val DarkSurface = Color(0xFF121212)     // Standardowy ciemny surface Material
val DarkSurfaceVariant = Color(0xFF1E1E1E) // Nieco jaśniejszy element (np. Card)

// Kolory tekstu
val TextWhite = Color(0xFFEEEEEE)
val TextGrey = Color(0xFFB0B0B0)